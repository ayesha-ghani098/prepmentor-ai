version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - cd client
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Building app...
      - npm run build

artifacts:
  files:
    - "**/*"
  base-directory: client/dist
